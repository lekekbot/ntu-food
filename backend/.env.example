# =====================================================
# NTU FOOD APP - ENVIRONMENT CONFIGURATION
# =====================================================

# Database Configuration
# For SQLite (local development):
# DATABASE_URL=sqlite:///./ntu_food.db
# For Supabase PostgreSQL (production):
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Supabase Configuration (optional)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_KEY=YOUR_SUPABASE_ANON_KEY

# Security Configuration
SECRET_KEY=your-super-secret-key-change-this-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Frontend Configuration
FRONTEND_URL=http://localhost:5174

# CORS Configuration (comma-separated list)
# For production, add your Vercel domain: https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Environment (development, staging, production)
ENVIRONMENT=development

# Email Configuration
EMAIL_TESTING_MODE=true  # Set to false to send real OTP emails
USE_SUPABASE_EMAIL=false  # Set to false to use Gmail SMTP (recommended)

# Gmail SMTP Configuration (for real email sending)
# To get Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-gmail@gmail.com
SMTP_PASSWORD=your-16-character-app-password
SMTP_FROM_NAME=NTU Food
APP_URL=http://localhost:5173

# =====================================================
# PRODUCTION DEPLOYMENT NOTES:
# =====================================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to git
# 3. Generate a secure SECRET_KEY for production (use: openssl rand -hex 32)
# 4. Configure Supabase PostgreSQL for DATABASE_URL
# 5. Set ENVIRONMENT=production
# 6. Update CORS_ORIGINS with your Vercel frontend URL
# 7. Configure real SMTP credentials for email OTP
# 8. Set EMAIL_TESTING_MODE=false for production
# =====================================================

# =====================================================
# RENDER.COM DEPLOYMENT (100% FREE):
# =====================================================
# Render Free Tier: https://render.com (No credit card required!)
#
# 1. Create Supabase PostgreSQL Database (Free):
#    - Go to https://supabase.com and create account
#    - Create new project
#    - Go to Settings > Database
#    - Copy "Connection string" in "URI" format
#    - Replace YOUR_PASSWORD with actual password
#    - Use this as DATABASE_URL in Render
#
# 2. Deploy to Render:
#    - Push code to GitHub
#    - Go to https://render.com
#    - New > Web Service > Connect Repository
#    - Render will auto-detect render.yaml
#    - Set these environment variables in Render dashboard:
#      * DATABASE_URL (from Supabase)
#      * SECRET_KEY (generate: openssl rand -hex 32)
#      * CORS_ORIGINS (your Vercel URL)
#      * SMTP_EMAIL (your Gmail)
#      * SMTP_PASSWORD (Gmail App Password)
#      * FRONTEND_URL (your Vercel URL)
#      * APP_URL (your Vercel URL)
#
# 3. Free Tier Sleep Mode:
#    - Render free tier sleeps after 15 min of inactivity
#    - Wakes up in ~30 seconds on first request
#    - Perfect for demo/MVP applications
#    - Optional: Use cron-job.org to ping every 14 min to keep awake
#
# Total Cost: $0/month (Render Free + Supabase Free + Vercel Free)
# =====================================================